[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
evm_version = "cancun"
optimizer = true
optimizer_runs = 200


remappings = [
    "@openzeppelin/=lib/openzeppelin-contracts/",
    "@chainlink/=lib/chainlink-evm/",
    "@forge-std/=lib/forge-std",
    "@forge-std/=lib/forge-std/src/"
]

[rpc_endpoints]
sepolia = "${SEPOLIA_RPC}"
mainnet = "${MAINNET_RPC}"

[etherscan]
sepolia = { key = "${PRIVATE_KEY}" }
mainnet = { key = "${ETHERSCAN_API_KEY}" }

[profile.ci]
optimizer_runs = 1

[profile.production]
optimizer_runs = 10000